#include <stdio.h>

int main() {
  //arithmetic operators

  // + (addition)
  // - (subtraction)
  // / (division)
  // * (multiplication)
  // % (modulus)
  // ++ (increment)
  // -- (decrement)

  int x = 5;
  int y = 2;

  // float z = x / (float) y;
  // int z = x%y;
  // x++;
  // y--; 
  
  return 0;
}