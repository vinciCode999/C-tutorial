#include <stdio.h>

int main(){
  /*
    AUGMENTED ASSIGNMENT OPERATORS = use to replace a statement where an operator takes a variable as one of its arguments
                                     and then assigns the result back to the same variable.
                                     x = x + 1
                                     x += 1
  */

  int x = 10;
  // x+=2;
  // x -= 3;
  // x *= 2;
  // x %= 5;
  // x /= 5;

  printf("%d", x);
  return 0;
}