#include <stdio.h>
#include <math.h>

int main(){
  double A = sqrt(9);
  double B = pow(4, 2);
  int C = round(3.1415);
  int D = ceil(3.14);
  int E = floor(3.99);
  double F = fabs(-100);
  double G = log(2);
  double H = sin(90);
  double I = cos(45);
  double J = tan(45);

  printf("%lf", J);

  return 0;
}